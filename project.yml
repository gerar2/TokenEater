name: TokenEater
options:
  bundleIdPrefix: com.tokeneater
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "15.0"
  groupSortPosition: top
  defaultLocalization: en

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: "S7B8M9JYF4"
    MARKETING_VERSION: "5.13.0"
    CURRENT_PROJECT_VERSION: "1"
    ARCHS: "arm64 x86_64"

targets:
  TokenEaterApp:
    type: application
    platform: macOS
    sources:
      - path: TokenEaterApp
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tokeneater.app
        PRODUCT_NAME: TokenEater
        CODE_SIGN_ENTITLEMENTS: TokenEaterApp/TokenEaterApp.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    info:
      path: TokenEaterApp/Info.plist
      properties:
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSApplicationCategoryType: public.app-category.utilities
        CFBundleDisplayName: TokenEater
        LSUIElement: true
        CFBundleURLTypes:
          - CFBundleURLName: com.tokeneater.app
            CFBundleURLSchemes:
              - tokeneater
    preBuildScripts:
      - script: |
          osacompile -o "$BUILT_PRODUCTS_DIR/$PRODUCT_NAME.app/Contents/Resources/TokenEaterInstaller.app" "$SRCROOT/TokenEaterApp/Resources/installer.applescript"
          cp "$SRCROOT/TokenEaterApp/Resources/te-update.sh" "$BUILT_PRODUCTS_DIR/$PRODUCT_NAME.app/Contents/Resources/TokenEaterInstaller.app/Contents/Resources/te-update.sh"
        name: Compile installer applets
    dependencies:
      - target: TokenEaterWidgetExtension
        embed: true

  TokenEaterTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: TokenEaterTests
      - path: Shared
      - path: TokenEaterApp/Onboarding/OnboardingViewModel.swift
      # Pure data type from the widget target, covered by UsageEntryTests.
      - path: TokenEaterWidget/UsageEntry.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tokeneater.tests
        CODE_SIGN_IDENTITY: ""
        CODE_SIGNING_REQUIRED: "NO"

  TokenEaterWidgetExtension:
    type: app-extension
    platform: macOS
    sources:
      - path: TokenEaterWidget
        excludes:
          - Info.plist
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tokeneater.app.widget
        PRODUCT_NAME: TokenEaterWidgetExtension
        CODE_SIGN_ENTITLEMENTS: TokenEaterWidget/TokenEaterWidget.entitlements
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks @executable_path/../../../../Frameworks"
    info:
      path: TokenEaterWidget/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension

schemes:
  TokenEaterTests:
    build:
      targets:
        TokenEaterTests: [test]
    test:
      config: Debug
      targets:
        - TokenEaterTests

  TokenEaterApp:
    build:
      targets:
        TokenEaterApp: all
        TokenEaterWidgetExtension: all
    run:
      config: Debug
    archive:
      config: Release
